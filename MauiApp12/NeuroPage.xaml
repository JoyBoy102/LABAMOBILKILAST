<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp12.NeuroPage"
             Title="NeuroPage">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="0.2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackLayout Grid.Row="0" Grid.Column="0">
            <Image x:Name="SelectedImage" WidthRequest="300" HeightRequest="300" Aspect="AspectFit"></Image>
            <Label x:Name="NeuroPredictionLabel" HeightRequest="20" WidthRequest="100" FontSize="16" FontAttributes="Bold"></Label>
        </StackLayout>
        <Button Text="Загрузить изображение" WidthRequest="200" HeightRequest="50" Clicked="Button_Clicked" Grid.Row="1"></Button>
    </Grid>
</ContentPage>